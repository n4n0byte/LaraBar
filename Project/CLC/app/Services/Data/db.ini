[User]
select="select *, id as ID from USERS where EMAIL = :email and binary PASSWORD = :password "
select.All="select *, id as ID from USERS "
create="insert into USERS (EMAIL, binary PASSWORD, FIRSTNAME, LASTNAME, ADMIN, AVATAR) values (:email, :password, :firstname, :lastname, 0, :avatar) "